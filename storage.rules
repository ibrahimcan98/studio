rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow teachers to read, write, update and delete their own profile files
    match /teacher-profiles/{userId}/{allPaths=**} {
      allow read, write, update, delete: if request.auth != null && request.auth.uid == userId;
    }
  }
}
